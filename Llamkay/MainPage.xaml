<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Llamkay.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Llamkay - Job Board">

    <Grid>
        <ScrollView>
            <VerticalStackLayout
                Padding="20"
                Spacing="15">

                <!--  Header  -->
                <Border
                    Padding="20"
                    BackgroundColor="{StaticResource Primary}"
                    StrokeThickness="0">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="10"/>
                    </Border.StrokeShape>
                    <VerticalStackLayout Spacing="5">
                        <Label
                            FontAttributes="Bold"
                            FontSize="24"
                            HorizontalOptions="Center"
                            Text="🦙 Llamkay Job Board"
                            TextColor="White"/>
                        <Label
                            FontSize="14"
                            HorizontalOptions="Center"
                            Text="Powered by The Muse API"
                            TextColor="White"/>
                    </VerticalStackLayout>
                </Border>

                <!--  API Test Section  -->
                <Border
                    Padding="15"
                    BackgroundColor="{StaticResource Secondary}"
                    Stroke="{StaticResource Gray200}"
                    StrokeThickness="1">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="8"/>
                    </Border.StrokeShape>
                    <VerticalStackLayout Spacing="10">
                        <Label
                            FontAttributes="Bold"
                            FontSize="18"
                            Text="🔧 API Test"/>
                        <Button
                            BackgroundColor="{StaticResource Primary}"
                            Command="{Binding TestApiCommand}"
                            Text="Test API Connection"
                            TextColor="White"/>
                    </VerticalStackLayout>
                </Border>

                <!--  Search Section  -->
                <Border
                    Padding="15"
                    BackgroundColor="{StaticResource Secondary}"
                    Stroke="{StaticResource Gray200}"
                    StrokeThickness="1">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="8"/>
                    </Border.StrokeShape>
                    <VerticalStackLayout Spacing="10">
                        <Label
                            FontAttributes="Bold"
                            FontSize="18"
                            Text="🔍 Job Search"/>

                        <Entry
                            Placeholder="Search keyword (e.g., developer, designer...)"
                            Text="{Binding SearchKeyword}"/>

                        <Grid
                            ColumnDefinitions="*,*"
                            ColumnSpacing="10">
                            <Picker
                                Title="Select Category"
                                Grid.Column="0"
                                ItemsSource="{Binding Categories}"
                                SelectedItem="{Binding SelectedCategory}"/>
                            <Picker
                                Title="Select Location"
                                Grid.Column="1"
                                ItemsSource="{Binding Locations}"
                                SelectedItem="{Binding SelectedLocation}"/>
                        </Grid>

                        <Grid
                            ColumnDefinitions="*,*,*"
                            ColumnSpacing="10">
                            <Button
                                Grid.Column="0"
                                BackgroundColor="{StaticResource Success}"
                                Command="{Binding SearchJobsCommand}"
                                Text="Search Jobs"
                                TextColor="White"/>
                            <Button
                                Grid.Column="1"
                                BackgroundColor="{StaticResource Info}"
                                Command="{Binding LoadCompaniesCommand}"
                                Text="Load Companies"
                                TextColor="White"/>
                            <Button
                                Grid.Column="2"
                                BackgroundColor="{StaticResource Warning}"
                                Command="{Binding ClearResultsCommand}"
                                Text="Clear"
                                TextColor="White"/>
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <!--  Status  -->
                <Border
                    Padding="10"
                    BackgroundColor="{StaticResource Gray100}"
                    Stroke="{StaticResource Gray200}"
                    StrokeThickness="1">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="5"/>
                    </Border.StrokeShape>
                    <Label
                        FontSize="14"
                        HorizontalOptions="Center"
                        Text="{Binding StatusMessage}"/>
                </Border>
                <!--  Jobs Results  -->
                <StackLayout
                    Padding="15"
                    BackgroundColor="Pink"
                    IsVisible="True">

                    <StackLayout Orientation="Horizontal"
                            Spacing="10">
                        <Label
                            FontAttributes="Bold"
                            FontSize="18"
                            Text="💼 Job Results"
                            VerticalOptions="Center"/>
                        <Label
                            FontSize="14"
                            Text="{Binding Jobs.Count, StringFormat='({0} jobs found)'}"
                            TextColor="Red"
                            VerticalOptions="Center"/>
                    </StackLayout>

                    <Label
                        FontSize="14"
                        Text="{Binding HasJobs, StringFormat='HasJobs: {0}'}"
                        TextColor="Blue"/>
                    <CollectionView
                        ItemsSource="{Binding Jobs}"
                        HeightRequest="400"
                        BackgroundColor="LightGray">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <StackLayout
                                    Padding="10"
                                    BackgroundColor="White"
                                    Margin="5">
                                    <Label
                                        FontAttributes="Bold"
                                        FontSize="16"
                                        Text="{Binding Name}"
                                        BackgroundColor="Yellow"/>
                                    <Label
                                        FontSize="14"
                                        Text="{Binding Company.Name}"
                                        BackgroundColor="LightBlue"/>
                                    <Label
                                        FontSize="12"
                                        Text="{Binding Type}"
                                        BackgroundColor="LightGreen"/>
                                </StackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </StackLayout>

                <!--  Companies Results  -->
                <Border
                    Padding="15"
                    BackgroundColor="{StaticResource Secondary}"
                    IsVisible="{Binding HasCompanies}"
                    Stroke="{StaticResource Gray200}"
                    StrokeThickness="1">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="8"/>
                    </Border.StrokeShape>
                    <VerticalStackLayout Spacing="10">
                        <StackLayout Orientation="Horizontal"
                                Spacing="10">
                            <Label
                                FontAttributes="Bold"
                                FontSize="18"
                                Text="🏢 Company Results"
                                VerticalOptions="Center"/>
                            <Label
                                FontSize="14"
                                Text="{Binding Companies.Count, StringFormat='({0} companies found)'}"
                                TextColor="{StaticResource Primary}"
                                VerticalOptions="Center"/>
                        </StackLayout>
                        <CollectionView
                            ItemsSource="{Binding Companies}"
                            MaximumHeightRequest="400"
                            BackgroundColor="Transparent"
                            SelectionMode="None">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Border
                                        Padding="15"
                                        Margin="5"
                                        BackgroundColor="White"
                                        Stroke="{StaticResource Gray300}"
                                        StrokeThickness="1">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="8"/>
                                        </Border.StrokeShape>
                                        <StackLayout Spacing="8">
                                            <Label
                                                FontAttributes="Bold"
                                                FontSize="16"
                                                Text="{Binding Name}"
                                                TextColor="Black"
                                                LineBreakMode="WordWrap"/>
                                            <Label
                                                FontSize="12"
                                                Text="{Binding ShortDescription}"
                                                TextColor="{StaticResource Gray600}"
                                                LineBreakMode="WordWrap"
                                                MaxLines="3"/>
                                        </StackLayout>
                                    </Border>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </Border>

            </VerticalStackLayout>
        </ScrollView>

        <!--  Loading Overlay  -->
        <Grid
            BackgroundColor="#80000000"
            IsVisible="{Binding IsLoading}">
            <Border
                BackgroundColor="White"
                HeightRequest="100"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                WidthRequest="200">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="10"/>
                </Border.StrokeShape>
                <VerticalStackLayout
                    HorizontalOptions="Center"
                    Spacing="15"
                    VerticalOptions="Center">
                    <ActivityIndicator
                        IsRunning="True"
                        Color="{StaticResource Primary}"/>
                    <Label
                        HorizontalOptions="Center"
                        Text="Loading..."/>
                </VerticalStackLayout>
            </Border>
        </Grid>
    </Grid>

</ContentPage>
